# Backend Environment Variables (Render)
# Copy these to your Render dashboard under Environment Variables

# Required: Generate a strong secret key
# Run: python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-here

# Required: PostgreSQL connection string (provided by Render)
DATABASE_URL=postgresql://user:password@host:port/database

# Required: Your Vercel frontend URL (add after deploying frontend)
FRONTEND_URL=https://your-app.vercel.app

# Optional: Python version
PYTHON_VERSION=3.12.3

